import { validateType } from '../utils/index';

describe('validation ', () => {
  // test('two plus two is four', () => {
  //   expect(2 + 2).toBe(4);
  // });
  test('validateType should return a boolean', () => {});
});
